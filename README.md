# Cloud URL Shortener

## Tech
Node.js, Express, MongoDB Atlas, Vercel

## Setup (local)
1. Clone repository
2. Create `.env` with `MONGO_URI=your_mongo_uri` (do NOT commit)
3. Install:
   ```bash
   npm install
